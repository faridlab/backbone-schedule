//! ScheduleEmployee seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial ScheduleEmployee data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SCHEDULEEMPLOYEE SEEDER
// ============================================================================

/// Seeder for schedule_employee
pub struct SeedScheduleEmployeeSeeder;

impl SeedScheduleEmployeeSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedScheduleEmployeeSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedScheduleEmployeeSeeder {
    fn name(&self) -> &'static str {
        "SeedScheduleEmployeeSeeder"
    }

    fn order(&self) -> i32 {
        2
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if schedule.schedule_employees table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM schedule.schedule_employees")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/schedule_employee_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM schedule.schedule_employees")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
